name: gis-app

services:
  postgres:
    image: postgres
    container_name: gis-app-postgres
    environment:
      POSTGRES_DB: gis-app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - gis-app_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  gis-app_postgres_data:
